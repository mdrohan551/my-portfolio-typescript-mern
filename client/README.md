🇧🇩 প্রজেক্টের বিবরণ এবং প্রযুক্তিগত ভিত্তি
এই প্রকল্পটি Vite বিল্ড টুল ব্যবহার করে নির্মিত একটি আধুনিক React অ্যাপ্লিকেশন। দ্রুত উন্নয়ন (Fast Refresh) এবং পারফরম্যান্স নিশ্চিত করার জন্য এই সেটআপটি TypeScript এবং Redux Toolkit-এর শক্ত ভিত্তির উপর তৈরি করা হয়েছে।

১. কেন TypeScript ব্যবহার করা হয়েছে?
আমাদের প্রজেক্টে JavaScript-এর পরিবর্তে TypeScript ব্যবহার করার মূল কারণ হলো:

নিরাপত্তা (Type Safety): এটি কোডের কম্পাইল-টাইমে (compile-time) টাইপ এরর খুঁজে বের করতে সাহায্য করে, যা প্রোডাকশনে যাওয়ার আগে বাগ (bug) কমায়।

স্বচ্ছতা (Clarity): এটি ডেটা স্ট্রাকচার (যেমন, props, state, API response) কে আরও সুস্পষ্ট করে তোলে, যা ডেভেলপারদের জন্য কোড বুঝতে এবং রক্ষণাবেক্ষণ করতে সহজ করে।

উন্নত ডেভেলপার অভিজ্ঞতা: আধুনিক IDE (যেমন VS Code)-তে Autocompletion এবং রিফ্যাক্টরিং (refactoring) অনেক উন্নত হয়।

২. স্টেট ম্যানেজমেন্ট: Redux Toolkit (RTK) ব্যবহার
অ্যাপ্লিকেশনের জটিল স্টেট (Global State) ম্যানেজ করার জন্য আমরা Redux Toolkit ব্যবহার করেছি। Redux Toolkit-এর প্রধান সুবিধাগুলি হলো:

কম boilerplate code: configureStore এবং createSlice-এর মাধ্যমে Redux সেটআপ অনেক সহজ হয়ে যায়।

Immer Integration: এটি Reducer-এর মধ্যে immutable update-কে mutable syntax-এ লেখার সুযোগ দেয়, যা কোডকে আরও সহজ করে তোলে।

RTK Query: ডেটা ফেচিং এবং ক্যাচিং-এর মতো অ্যাসিঙ্ক্রোনাস অপারেশনগুলি পরিচালনার জন্য আমরা RTK Query ব্যবহার করেছি, যা সার্ভার স্টেট ম্যানেজমেন্টকে অবিশ্বাস্যভাবে সহজ করে।

৩. কাস্টম টাইপড হুক (useAppSelector ও useAppDispatch)
আমাদের প্রজেক্টে সরাসরি useSelector এবং useDispatch ব্যবহার না করে, আমরা src/app/hooks.ts ফাইলে দুটি কাস্টম হুক তৈরি করেছি: useAppSelector এবং useAppDispatch।

কেন এই কাস্টম হুক ব্যবহার করা হলো?

টাইপ ইনফ্যারেন্স (Type Inference): এই কুকগুলি store.ts থেকে RootState এবং AppDispatch টাইপগুলি গ্রহণ করে। এর ফলে, আমাদের অ্যাপ্লিকেশনের যেকোনো কম্পোনেন্টে যখনই আমরা useAppSelector ব্যবহার করি, তখন TypeScript স্বয়ংক্রিয়ভাবে Global State-এর সঠিক কাঠামো (Structure) বুঝে নেয়।

কম্পোনেন্টের কোড পরিষ্কার রাখা: এর ফলে প্রতিটি কম্পোনেন্টে ম্যানুয়ালি টাইপ ইমপোর্ট এবং ডিফাইন করার ঝামেলা থেকে মুক্তি পাওয়া যায়। এটি কোডকে পরিষ্কার, DRY (Don't Repeat Yourself) এবং Type-Safe রাখে।

প্রজেক্ট চালানোর নির্দেশিকা (Running the Project)
প্রজেক্টটি লোকাল মেশিনে সেটআপ করার জন্য নিম্নলিখিত কমান্ডগুলি ব্যবহার করুন:

ডিপেন্ডেন্সি ইন্সটল করুন:

Bash
npm install
npm run dev
npm run build
